services:
  game-night-bot:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /data/games.db
      # LLM_BASE_URL: http://host.docker.internal:1234/v1
    volumes:
      - ./data:/data
    restart: unless-stopped
